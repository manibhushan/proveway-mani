query RunInput {
  shop {
    metafield(namespace: "volume_discount", key: "rules") {
      value
    }
  }
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            id
          }
        }
      }
    }
  }
}
